export function updateTree (state, tree) {
  state.tree = tree
}

export function updatePath (state, path) { state.path = path }
// export function updateCurrentProject (state, project) { state.project = project }
