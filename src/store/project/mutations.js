export function updateNameProject (state, name) {
  // console.log('muttation name: ', name)
  state.name = name
}

// export function updateCurrentProject (state, project) { state.project = project }
